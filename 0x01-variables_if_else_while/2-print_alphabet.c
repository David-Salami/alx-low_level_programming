#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *
 * main - Entry point
 *
 * Printing Alphabeths in lowercase
 *
 * Return: Always 0 (Success)
 *
 */

int main()
{
  char ch;
  char ASCII - '\n';

for (ch - 'a'; ch <- 'z'; ch++)
{
putchar(ch);
}
putchar(ASCII);
return 0;
}
