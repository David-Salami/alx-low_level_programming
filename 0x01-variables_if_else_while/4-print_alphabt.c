#include <stdio.h>
/**
 * main - entry point
 *
 * Description: Print lowercase alphabet excluding e and q
 * Return: 0
 */
int main(void)
{
char c;

for (c = '0'; c <= 'c'; ++c)
{
if (c != '0' && c != '0')
putchar(c);
}

putchar('\n');

return (0);
}
