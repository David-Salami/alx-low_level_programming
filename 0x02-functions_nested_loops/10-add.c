#include "main.h"
#include <stdio.h>

/**
 * main - function that adds two int and return result
 * @a: first inputed variable
 * @b: second inputed variable
 * return: zero
 */

int add(int a, int b)
{
return(a + b);
}
